{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "./node_modules/.bin/prisma db push --skip-generate && ./node_modules/.bin/prisma db seed && export NODE_OPTIONS=--dns-result-order=ipv4first && exec ./node_modules/.bin/next start -H 0.0.0.0 -p ${PORT:-3000}",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5
  }
}
